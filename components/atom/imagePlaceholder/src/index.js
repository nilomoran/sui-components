import React, {Component} from 'react'
// import PropTypes from 'prop-types'

class AtomImagePlaceholder extends Component {
  render () {
    return (
      <div className='react-AtomImagePlaceholder'>
        <h1>AtomImagePlaceholder</h1>
      </div>
    )
  }
}

AtomImagePlaceholder.displayName = 'AtomImagePlaceholder'

// Remove these comments if you need
// AtomImagePlaceholder.contextTypes = {i18n: PropTypes.object}
// AtomImagePlaceholder.propTypes = {}
// AtomImagePlaceholder.defaultProps = {}

export default AtomImagePlaceholder